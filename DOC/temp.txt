Common

	Solve ANSI/Unicode model compile warning and error.
	Solve GAIA_DEBUG_MEMORYLEAK's compile warning and error.
	Solve 32/64 bit compile warning and error.


Container
	Book's test case impl.


Render

	Gdiplus support target function.
	Gdiplus support texture function.
	Gdiplus support alpha blend enhance function.
	
	Shader system designment.


Timer
	Timer implement.


Time
	toyear change to year.


Math
	MTX33, MTX22, MTX43 implement.


FRAMEWORK
	All Instance derived class's destructor or RefObjectDestruct will destroy itself completely.


NETWORK
	Remove network test case's xsleep function.


LOG
	Add global log system.


EXCEPTION
	Add exception system.
	Accesser will cause exception(OutOfBounder).
	BasicChars will cause exception(NotEnoughMem).
	BasicArray will cause exception(NotEnoughMem).
	BasicStackQueue will cause exception(NotEnoughMem).
	BasicStackStack will cause exception(NotEnoughMem).
	BasicStackPackage will cause exception(NotEnoughMem).
	BasicStackBitset will cause exception(NotEnoughMem).


DataResearch
	Pair operation end's special operations.
	Change U64 to N64.
	
	
24198